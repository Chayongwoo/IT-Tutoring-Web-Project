@model IEnumerable<WebProject_ITTaling.Models.Review>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Lecture.lectureTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.reviewContent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.reviewGrade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.memberID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Lecture.lectureTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.reviewContent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.reviewGrade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.memberID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.reviewID }) |
            @Html.ActionLink("Details", "Details", new { id=item.reviewID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.reviewID })
        </td>
    </tr>
}

</table>
